import React from 'react';

const Header = () => {
    return (
        <header>
            <h1 style={{margin: 0, fontWeight: "normal"}}>TVVS TODO-List</h1>
        </header>
    );
};

export default Header;